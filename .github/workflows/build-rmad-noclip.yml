name: Build RMAD NoClip

on:
  workflow_dispatch:
  push:
    paths:
      - 'noclip/**'
      - '.github/workflows/build-rmad-noclip.yml'

jobs:
  build:
    runs-on: windows-2022
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Get ScriptHookV SDK from public template
        shell: pwsh
        run: |
          git clone --depth 1 https://github.com/usrbinbryce/scripthookv_template.git sdk-template
          New-Item -ItemType Directory -Force -Path noclip/sdk/inc | Out-Null
          New-Item -ItemType Directory -Force -Path noclip/sdk/lib | Out-Null
          Copy-Item sdk-template/extern/scripthookv/inc/* noclip/sdk/inc/ -Recurse -Force
          Copy-Item sdk-template/extern/scripthookv/lib/* noclip/sdk/lib/ -Recurse -Force

      - name: Build x64 Release
        shell: pwsh
        run: msbuild noclip/RMAD_NoClip.vcxproj /p:Configuration=Release /p:Platform=x64 /m

      - name: Package ASI
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path package | Out-Null
          Copy-Item noclip/bin/RMAD_NoClip.dll package/RMAD_NoClip.asi
          @"
          RMAD NoClip
          -----------
          F2       : Toggle NoClip
          W/A/S/D  : Move
          Space    : Up
          Ctrl     : Down
          Shift    : Fast
          Alt      : Slow

          Requires ScriptHookV.dll + dinput8.dll in the GTA V folder.
          Story Mode only. Do not use in GTA Online.
          "@ | Set-Content package/README.txt -Encoding UTF8

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: RMAD-NoClip-Ready
          path: package/*
